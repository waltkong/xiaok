import 'package:flutter/material.dart';

class RankListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Text('RankListPage'),
    );
  }
}